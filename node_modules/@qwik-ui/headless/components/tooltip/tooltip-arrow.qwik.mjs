import { jsx } from "@builder.io/qwik/jsx-runtime";
import { component$ } from "@builder.io/qwik";
const HTooltipArrow = component$((props) => {
  const { width = 10, height = 5 } = props;
  return /* @__PURE__ */ jsx("div", {
    class: [
      "tooltip-arrow",
      props.class
    ],
    style: {
      width: `${width}px`,
      height: `${height}px`
    }
  });
});
export {
  HTooltipArrow
};
