import { createContextId } from "@builder.io/qwik";
const accordionContextId = createContextId("qui-accordion");
const accordionItemContextId = createContextId("qui-accordion-item");
export {
  accordionContextId,
  accordionItemContextId
};
