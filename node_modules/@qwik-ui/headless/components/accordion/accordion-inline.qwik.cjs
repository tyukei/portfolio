"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const jsxRuntime = require("@builder.io/qwik/jsx-runtime");
const accordionRoot = require("./accordion-root.qwik.cjs");
const inlineComponent = require("../../utils/inline-component.qwik.cjs");
const accordionItem = require("./accordion-item.qwik.cjs");
const HAccordionRoot = (props) => {
  const { children, accordionItemComponent: GivenItem, value: initialValue, ...rest } = props;
  const Item = GivenItem || accordionItem.HAccordionItem;
  let currItemIndex = 0;
  let initialIndex = null;
  const itemsMap = /* @__PURE__ */ new Map();
  inlineComponent.findComponent(Item, (itemProps) => {
    itemProps._index = currItemIndex;
    itemsMap.set(currItemIndex, itemProps.disabled);
    if (initialValue && initialValue === itemProps.value) {
      initialIndex = currItemIndex;
    }
    currItemIndex++;
  });
  inlineComponent.processChildren(children);
  return /* @__PURE__ */ jsxRuntime.jsx(accordionRoot.HAccordionRootImpl, {
    initialIndex: initialIndex ?? void 0,
    itemsMap,
    ...rest,
    children: props.children
  });
};
exports.HAccordionRoot = HAccordionRoot;
