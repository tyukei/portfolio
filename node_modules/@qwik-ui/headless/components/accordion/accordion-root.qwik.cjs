"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const jsxRuntime = require("@builder.io/qwik/jsx-runtime");
const qwik = require("@builder.io/qwik");
const accordionContext = require("./accordion-context.qwik.cjs");
const HAccordionRootImpl = qwik.component$((props) => {
  const { multiple, "bind:value": givenValueSig, initialIndex, onChange$, disabled, collapsible = true, animated, itemsMap, ...rest } = props;
  const selectedIndexSig = qwik.useSignal(initialIndex ?? -1);
  const triggerRefsArray = qwik.useSignal([]);
  const isAnimatedSig = qwik.useSignal(animated === true);
  const isMultipleSig = qwik.useSignal(multiple || props.behavior === "multi");
  const itemsMapSig = qwik.useComputed$(() => {
    return props.itemsMap;
  });
  const context = {
    selectedIndexSig,
    givenValueSig,
    isMultipleSig,
    initialIndex,
    onChange$,
    itemsMapSig,
    triggerRefsArray,
    disabled,
    collapsible,
    isAnimatedSig
  };
  qwik.useTask$(({ track }) => {
    context.disabled = track(() => disabled);
  });
  qwik.useContextProvider(accordionContext.accordionContextId, context);
  return /* @__PURE__ */ jsxRuntime.jsx("div", {
    ...rest,
    "data-accordion": true,
    children: /* @__PURE__ */ jsxRuntime.jsx(qwik.Slot, {})
  });
});
exports.HAccordionRootImpl = HAccordionRootImpl;
