import { PropsOf } from '@builder.io/qwik';
import { FunctionComponent } from '@builder.io/qwik/jsx-runtime';
/**
 *  We can use polymorphic components to render different elements based on the type of the `as` prop.
 */
export declare const Polymorphic: <C extends string | FunctionComponent = "div">(props: import('@builder.io/qwik').PublicProps<{
    as?: C;
} & PropsOf<string extends C ? "div" : C>>, key: string | null, flags: number, dev?: import('@builder.io/qwik').DevJSX) => import('@builder.io/qwik').JSXOutput;
