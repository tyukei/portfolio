"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const jsxRuntime = require("@builder.io/qwik/jsx-runtime");
const qwik = require("@builder.io/qwik");
const ORIENTATIONS = [
  "horizontal",
  "vertical"
];
const HSeparator = qwik.component$(({ orientation: orientationProp = "horizontal", decorative, ...props }) => {
  const orientation = qwik.useComputed$(() => {
    if (ORIENTATIONS.includes(orientationProp)) {
      return orientationProp;
    }
    console.warn(`Invalid prop 'orientation' of value '${orientationProp}' supplied to 'separator',
        expected one of:
        - horizontal
        - vertical

        Defaulting to 'horizontal'.`);
    return "horizontal";
  });
  const ariaOrientation = qwik.useComputed$(() => orientation.value === "vertical" ? orientation.value : void 0);
  const semanticProps = qwik.useComputed$(() => decorative ? {
    role: "none"
  } : {
    role: "separator",
    "aria-orientation": ariaOrientation.value
  });
  return /* @__PURE__ */ jsxRuntime.jsx("div", {
    "data-orientation": orientation.value,
    ...semanticProps.value,
    ...props
  });
});
exports.HSeparator = HSeparator;
