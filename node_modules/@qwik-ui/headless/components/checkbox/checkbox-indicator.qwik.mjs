import { jsx } from "@builder.io/qwik/jsx-runtime";
import { component$, useStyles$, useContext, useTask$, Slot } from "@builder.io/qwik";
import { CheckboxContext } from "./checkbox-context.qwik.mjs";
/* empty css                   */
import styles from "./checkbox.css.qwik2.mjs";
const CheckboxIndicator = component$((props) => {
  useStyles$(styles);
  const checkSig = useContext(CheckboxContext);
  useTask$(({ track }) => {
    track(() => checkSig.value);
  });
  return /* @__PURE__ */ jsx("div", {
    ...props,
    "data-hidden": !checkSig.value,
    "data-qds-indicator": true,
    "aria-hidden": "true",
    children: /* @__PURE__ */ jsx(Slot, {})
  });
});
export {
  CheckboxIndicator
};
