"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const jsxRuntime = require("@builder.io/qwik/jsx-runtime");
const qwik = require("@builder.io/qwik");
const tab = require("./tab.qwik.cjs");
const tabsContextId = require("./tabs-context-id.qwik.cjs");
const TAB_PANEL_ID_PREFIX = "_tabpanel_";
const HTabPanel = qwik.component$(
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  ({ label, _tabId, _extraClass, ...props }) => {
    const contextService = qwik.useContext(tabsContextId.tabsContextId);
    const fullPanelElementId = contextService.tabsPrefix + TAB_PANEL_ID_PREFIX + _tabId;
    const fullTabElementId = contextService.tabsPrefix + tab.TAB_ID_PREFIX + _tabId;
    const isSelectedSig = qwik.useComputed$(() => {
      return contextService.selectedTabIdSig.value === _tabId;
    });
    return /* @__PURE__ */ jsxRuntime.jsx("div", {
      ...props,
      id: fullPanelElementId,
      "aria-labelledby": fullTabElementId,
      role: "tabpanel",
      tabIndex: 0,
      hidden: !isSelectedSig.value,
      children: /* @__PURE__ */ jsxRuntime.jsx(qwik.Slot, {})
    });
  }
);
exports.HTabPanel = HTabPanel;
exports.TAB_PANEL_ID_PREFIX = TAB_PANEL_ID_PREFIX;
