import { PropsOf } from '@builder.io/qwik';
export type TabPanelProps = {
    /** Optional tab contents. */
    /** @deprecated This prop was used for the shorthand API that we decided to no longer support as part of the headless kit */
    label?: PropsOf<'div'>['children'];
    selected?: boolean;
    disabled?: boolean;
    /** @deprecated Internal use only */
    _tabId?: string;
    /** @deprecated Internal use only */
    _extraClass?: PropsOf<'div'>['class'];
} & PropsOf<'div'>;
export declare const TAB_PANEL_ID_PREFIX = "_tabpanel_";
export declare const HTabPanel: import('@builder.io/qwik').Component<TabPanelProps>;
