import { jsx } from "@builder.io/qwik/jsx-runtime";
import { component$, useContext, useContextProvider, Slot } from "@builder.io/qwik";
import { comboboxContextId, groupContextId } from "./combobox-context.qwik.mjs";
import { useCombinedRef } from "../../hooks/combined-refs.qwik.mjs";
const HComboboxGroup = component$((props) => {
  const context = useContext(comboboxContextId);
  const groupLabelId = `${context.localId}-group-label`;
  const contextRefOpts = {
    context,
    givenContextRef: context.groupRef
  };
  const groupRef = useCombinedRef(props.ref, contextRefOpts);
  const groupContext = {
    groupLabelId
  };
  useContextProvider(groupContextId, groupContext);
  return /* @__PURE__ */ jsx("div", {
    "aria-labelledby": groupLabelId,
    role: "group",
    ...props,
    ref: groupRef,
    children: /* @__PURE__ */ jsx(Slot, {})
  });
});
export {
  HComboboxGroup
};
