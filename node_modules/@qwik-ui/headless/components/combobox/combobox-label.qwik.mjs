import { jsx } from "@builder.io/qwik/jsx-runtime";
import { component$, useContext, $, Slot } from "@builder.io/qwik";
import { comboboxContextId } from "./combobox-context.qwik.mjs";
import { HLabel } from "../label/label.qwik.mjs";
import { useCombinedRef } from "../../hooks/combined-refs.qwik.mjs";
const HComboboxLabel = component$((props) => {
  const context = useContext(comboboxContextId);
  const labelId = `${context.localId}-label`;
  const contextRefOpts = {
    context,
    givenContextRef: context.labelRef
  };
  const labelRef = useCombinedRef(props.ref, contextRefOpts);
  const handleClick$ = $(() => {
    context.inputRef.value?.focus();
  });
  return /* @__PURE__ */ jsx(HLabel, {
    onClick$: handleClick$,
    id: labelId,
    ref: labelRef,
    ...props,
    children: /* @__PURE__ */ jsx(Slot, {})
  });
});
export {
  HComboboxLabel
};
