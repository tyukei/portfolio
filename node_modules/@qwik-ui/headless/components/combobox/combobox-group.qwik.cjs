"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const jsxRuntime = require("@builder.io/qwik/jsx-runtime");
const qwik = require("@builder.io/qwik");
const comboboxContext = require("./combobox-context.qwik.cjs");
const combinedRefs = require("../../hooks/combined-refs.qwik.cjs");
const HComboboxGroup = qwik.component$((props) => {
  const context = qwik.useContext(comboboxContext.comboboxContextId);
  const groupLabelId = `${context.localId}-group-label`;
  const contextRefOpts = {
    context,
    givenContextRef: context.groupRef
  };
  const groupRef = combinedRefs.useCombinedRef(props.ref, contextRefOpts);
  const groupContext = {
    groupLabelId
  };
  qwik.useContextProvider(comboboxContext.groupContextId, groupContext);
  return /* @__PURE__ */ jsxRuntime.jsx("div", {
    "aria-labelledby": groupLabelId,
    role: "group",
    ...props,
    ref: groupRef,
    children: /* @__PURE__ */ jsxRuntime.jsx(qwik.Slot, {})
  });
});
exports.HComboboxGroup = HComboboxGroup;
