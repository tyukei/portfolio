import { createContextId } from "@builder.io/qwik";
const comboboxContextId = createContextId("qui-combobox");
const groupContextId = createContextId("qui-combobox-group");
const comboboxItemContextId = createContextId("qui-combobox-item");
export {
  comboboxContextId,
  comboboxItemContextId,
  groupContextId
};
