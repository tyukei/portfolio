import { jsx } from "@builder.io/qwik/jsx-runtime";
import { component$, useContext, Slot } from "@builder.io/qwik";
import { comboboxItemContextId } from "./combobox-context.qwik.mjs";
const HComboboxItemIndicator = component$((props) => {
  const itemContext = useContext(comboboxItemContextId);
  return /* @__PURE__ */ jsx("span", {
    "data-selected": itemContext.isSelectedSig.value ? "" : void 0,
    "data-item-indicator": true,
    "aria-hidden": "true",
    ...props,
    children: /* @__PURE__ */ jsx(Slot, {})
  });
});
export {
  HComboboxItemIndicator
};
