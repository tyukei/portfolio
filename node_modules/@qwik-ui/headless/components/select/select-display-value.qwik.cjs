"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const jsxRuntime = require("@builder.io/qwik/jsx-runtime");
const qwik = require("@builder.io/qwik");
const selectContext = require("./select-context.qwik.cjs");
const HSelectDisplayValue = qwik.component$((props) => {
  const { placeholder, ...rest } = props;
  const context = qwik.useContext(selectContext.default);
  const valueId = `${context.localId}-value`;
  const displayStrSig = qwik.useComputed$(() => {
    if (context.multiple) {
      return /* @__PURE__ */ jsxRuntime.jsx(qwik.Slot, {});
    }
    if (context.selectedIndexSetSig.value.size === 0) {
      return placeholder;
    } else {
      return context.multiple ? context.currDisplayValueSig.value : context.currDisplayValueSig.value?.[0] ?? placeholder;
    }
  });
  return /* @__PURE__ */ jsxRuntime.jsx("span", {
    id: valueId,
    "data-open": context.isListboxOpenSig.value ? "" : void 0,
    "data-closed": !context.isListboxOpenSig.value ? "" : void 0,
    "data-value": true,
    ...rest,
    children: displayStrSig.value
  });
});
exports.HSelectDisplayValue = HSelectDisplayValue;
