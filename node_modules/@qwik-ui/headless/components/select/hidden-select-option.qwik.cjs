"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const jsxRuntime = require("@builder.io/qwik/jsx-runtime");
const qwik = require("@builder.io/qwik");
const build = require("@builder.io/qwik/build");
const selectContext = require("./select-context.qwik.cjs");
const HiddenSelectOption = qwik.component$(({ value, displayValue, nativeSelectRef, index, ...rest }) => {
  const optionRef = qwik.useSignal();
  const context = qwik.useContext(selectContext.default);
  qwik.useTask$(async function modularFormsValidation({ track }) {
    track(() => context.selectedIndexSetSig.value);
    if (build.isServer || !nativeSelectRef.value || !optionRef.value) return;
    const inputEvent = new Event("input", {
      bubbles: false
    });
    nativeSelectRef.value?.dispatchEvent(inputEvent);
    optionRef.value.selected = context.selectedIndexSetSig.value.has(index);
  });
  return /* @__PURE__ */ jsxRuntime.jsx("option", {
    ref: optionRef,
    value,
    ...rest,
    children: displayValue
  });
});
exports.HiddenSelectOption = HiddenSelectOption;
