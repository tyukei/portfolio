"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const jsxRuntime = require("@builder.io/qwik/jsx-runtime");
const dropdownContext = require("./dropdown-context.qwik.cjs");
const qwik = require("@builder.io/qwik");
const HDropdownRadioGroup = qwik.component$((props) => {
  const { disabled = false, value = "", onChange$, ...rest } = props;
  const valueSig = qwik.useSignal(value);
  qwik.useTask$(function reactiveUserValue({ track }) {
    const bindValueSig = props["bind:value"];
    if (!bindValueSig) return;
    track(() => bindValueSig.value);
    valueSig.value = bindValueSig.value;
  });
  qwik.useTask$(function onChangeTask({ track }) {
    track(() => valueSig.value);
    onChange$?.(valueSig.value);
  });
  const radioGroupContext = {
    valueSig,
    disabled
  };
  qwik.useContextProvider(dropdownContext.dropdownRadioGroupContextId, radioGroupContext);
  return /* @__PURE__ */ jsxRuntime.jsx("div", {
    role: "radiogroup",
    ...rest,
    "data-disabled": disabled,
    children: /* @__PURE__ */ jsxRuntime.jsx(qwik.Slot, {})
  });
});
exports.HDropdownRadioGroup = HDropdownRadioGroup;
