import { QRL, Signal } from '@builder.io/qwik';
import { DropdownItemProps } from './dropdown-item';
type DropdownRadioGroupProps = {
    /** A signal that controls the current group value (controlled). */
    'bind:value'?: Signal<string>;
    disabled?: boolean;
    value?: string;
    /**
     * QRL handler that runs when the user selects an item.
     */
    onChange$?: QRL<(value: string) => void>;
} & DropdownItemProps;
export declare const HDropdownRadioGroup: import('@builder.io/qwik').Component<DropdownRadioGroupProps>;
export {};
