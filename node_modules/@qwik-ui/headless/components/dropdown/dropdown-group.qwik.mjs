import { jsx } from "@builder.io/qwik/jsx-runtime";
import { dropdownContextId, dropdownGroupContextId } from "./dropdown-context.qwik.mjs";
import { component$, useContext, useContextProvider, Slot } from "@builder.io/qwik";
const HDropdownGroup = component$((props) => {
  const context = useContext(dropdownContextId);
  const groupLabelId = `${context.localId}-group-label`;
  const dropdownGroupContext = {
    groupLabelId
  };
  useContextProvider(dropdownGroupContextId, dropdownGroupContext);
  return /* @__PURE__ */ jsx("div", {
    "aria-labelledby": groupLabelId,
    role: "group",
    ...props,
    children: /* @__PURE__ */ jsx(Slot, {})
  });
});
export {
  HDropdownGroup
};
