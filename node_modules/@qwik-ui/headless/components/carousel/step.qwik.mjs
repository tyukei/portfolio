import { jsx, Fragment } from "@builder.io/qwik/jsx-runtime";
import { component$, useContext, useComputed$, $, Slot } from "@builder.io/qwik";
import { carouselContextId } from "./context.qwik.mjs";
const CarouselStep = component$((props) => {
  const context = useContext(carouselContextId);
  const { as, _index, ...rest } = props;
  const Comp = as ?? "button";
  const localIndexSig = useComputed$(() => _index ?? 0);
  const isCurrentSig = useComputed$(() => context.currentIndexSig.value === _index ? "step" : void 0);
  const handleClick$ = $(() => {
    context.currentIndexSig.value = localIndexSig.value;
  });
  return /* @__PURE__ */ jsx(Fragment, {
    children: /* @__PURE__ */ jsx(Comp, {
      "data-qui-carousel-step": true,
      "aria-current": isCurrentSig.value,
      "data-current": isCurrentSig.value ? "" : void 0,
      "data-step": localIndexSig.value + 1,
      ...Comp === "button" && {
        onClick$: [
          handleClick$,
          rest.onClick$
        ]
      },
      ...rest,
      children: /* @__PURE__ */ jsx(Slot, {})
    })
  });
});
export {
  CarouselStep
};
