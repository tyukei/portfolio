"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const jsxRuntime = require("@builder.io/qwik/jsx-runtime");
const root = require("./root.qwik.cjs");
const inlineComponent = require("../../utils/inline-component.qwik.cjs");
const slide = require("./slide.qwik.cjs");
const bullet = require("./bullet.qwik.cjs");
const step = require("./step.qwik.cjs");
const title = require("./title.qwik.cjs");
const CarouselRoot = (props) => {
  const { children, carouselSlideComponent: GivenSlideOld, carouselBulletComponent: GivenBulletOld, slideComponent: GivenSlide, bulletComponent: GivenBullet, stepComponent: GivenStep, titleComponent: GivenTitle, ...rest } = props;
  const Slide = GivenSlide || GivenSlideOld || slide.CarouselSlide;
  const Bullet = GivenBullet || GivenBulletOld || bullet.CarouselBullet;
  const Step = GivenStep || step.CarouselStep;
  const Title = GivenTitle || title.CarouselTitle;
  let currSlideIndex = 0;
  let currBulletIndex = 0;
  let currStepIndex = 0;
  let numSlides = 0;
  let isTitle = false;
  inlineComponent.findComponent(Slide, (slideProps) => {
    slideProps._index = currSlideIndex;
    currSlideIndex++;
    numSlides++;
  });
  inlineComponent.findComponent(Bullet, (bulletProps) => {
    bulletProps._index = currBulletIndex;
    currBulletIndex++;
  });
  inlineComponent.findComponent(Step, (stepProps) => {
    stepProps._index = currStepIndex;
    currStepIndex++;
  });
  inlineComponent.findComponent(Title, () => {
    isTitle = true;
  });
  inlineComponent.processChildren(children);
  return /* @__PURE__ */ jsxRuntime.jsx(root.CarouselBase, {
    _numSlides: numSlides,
    _isTitle: isTitle,
    ...rest,
    children: props.children
  });
};
exports.CarouselRoot = CarouselRoot;
