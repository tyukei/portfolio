import { jsx } from "@builder.io/qwik/jsx-runtime";
import { component$, useContext, $, Slot } from "@builder.io/qwik";
import { carouselContextId } from "./context.qwik.mjs";
const CarouselPlayer = component$((props) => {
  const context = useContext(carouselContextId);
  const handleClick$ = $(() => {
    context.isAutoplaySig.value = !context.isAutoplaySig.value;
  });
  return /* @__PURE__ */ jsx("button", {
    "aria-label": context.isAutoplaySig.value ? "stop automatic slide show" : "start automatic slide show",
    onClick$: [
      handleClick$,
      props.onClick$
    ],
    "data-qui-carousel-player": true,
    ...props,
    children: /* @__PURE__ */ jsx(Slot, {})
  });
});
export {
  CarouselPlayer
};
